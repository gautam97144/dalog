import 'package:flutter/material.dart';

class LanguageSelection {
  String? title;
  bool? isSelected;
  Locale? languageLocal;

  LanguageSelection({this.title, this.isSelected, this.languageLocal});
}
